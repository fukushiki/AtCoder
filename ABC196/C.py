import sys
import math

def resolve():
    N = int(input())
    n_len = len(str(N))
    ls = []
    while(n < N):
        if(n_len % 2 == 1):
            n = 10 * (n_len)
        
        for

    
    
# resolve()


